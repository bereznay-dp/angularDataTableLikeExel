import { KeyLisnDirective } from './key-lisn.directive';

describe('KeyLisnDirective', () => {
  it('should create an instance', () => {
    const directive = new KeyLisnDirective();
    expect(directive).toBeTruthy();
  });
});
